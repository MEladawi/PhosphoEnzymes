% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{validate_term_set}
\alias{validate_term_set}
\title{Structurally validate one or more term-set tables}
\usage{
validate_term_set(term_sets = NULL)
}
\arguments{
\item{term_sets}{\code{NULL} (validate the four shipped defaults) or a named list
of term-set data frames to validate.}
}
\value{
A \code{data.frame(severity, table, term_id, message)} -- one row per
issue, empty when clean. \code{severity == "error"} rows are fatal.
}
\description{
Mirrors the build-side structural checks (a subset -- no gene-set resolution
or canary checks) so a user can lint a hand-edited term-set table before
feeding it back. With no argument it loads and validates the four shipped
default CSVs; otherwise pass a named list (any subset of
\code{kinase_ec} / \code{kinase_go} / \code{phosphatase_ec} / \code{phosphatase_go}).
}
\examples{
validate_term_set()
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{provenance}
\alias{provenance}
\title{Report the term-set provenance carried by a reference table}
\usage{
provenance(x)
}
\arguments{
\item{x}{A table returned by \code{\link[=get_kinases]{get_kinases()}}, \code{\link[=get_phosphatases]{get_phosphatases()}}, or
\code{\link[=get_phosphoenzymes]{get_phosphoenzymes()}}.}
}
\value{
A list with \code{term_set_md5} (the named md5 vector) and, when readable,
\code{releases} (the source release strings from the build manifest).
}
\description{
The masters and the unified summary carry a \code{term_set_md5} attribute (the md5
fingerprints of the four default EC/GO term-set CSVs the build typed them
against). \code{provenance()} surfaces those fingerprints plus, when the \code{yaml}
package is available, the pinned source release strings from the shipped
build manifest.
}
\examples{
provenance(get_kinases())
}
